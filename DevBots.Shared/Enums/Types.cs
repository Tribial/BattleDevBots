using System;
using System.Collections.Generic;
using System.Text;

namespace DevBots.Shared.Enums
{
    public enum Types
    {
        STRING = 0,
        NUMBER,
        EXPRESSION,
        PRINT,
        LET,
        EQUALS,
        FUNC,
        END_FUNC,
        PARAMS,
        COMPARE,
        BOOLEAN,
        NOT,
        IF,
        ELSE,
        THEN,
        END_IF,
        NEWLINE,
        UNDEFINED,
        WHILE,
        END_WHILE,
        ROBOT,
    }
}
