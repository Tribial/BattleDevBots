using System;
using System.Collections.Generic;
using System.Text;

namespace DevBots.Shared.Enums
{
    public enum Types
    {
        STRING = 0,
        NUMBER,
        EXPRESSION,
        PRINT,
        LET,
        EQUALS,
        COMPARE,
        BOOLEAN,
        NOT,
        IF,
        ELSE,
        THEN,
        ENDIF,
        NEWLINE,
        UNDEFINED,
    }
}
